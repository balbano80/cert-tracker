import React from 'react';
import {SideNavCat} from 'mdbreact';






// 
// if results.id === ID_CHECK[i], 
// var siteArray=[];
// siteArray.push(results.id(ID_CHECK[i]))

const SideNavCatAdd = props => (
    // something .map to map through all of the indices of siteArray
    <SideNavCat to="#" className={classnames({ active: this.state.activeItemInnerPills === '2' + " current index of siteArray" })} id="sidenav-site" onClick={() => { this.toggleInnerPills('2 + currentIndex of siteArray'); }} name="siteArray" icon="building-o"></SideNavCat>
)

